@model PropertyFinder.Data.Models.Message

@{
    ViewBag.Title = "Request Message";
    Layout = "~/Areas/Owner/Views/Shared/_Layout.cshtml"; }

<h2>Message</h2>

<main id="main">
    <section class="intro-single">
        <div class="card">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-sm-6">
                        <div class="my-auto">
                            <form asp-action="SendMessage" method="post">

                                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                                <input asp-for="TenantId" type="hidden" class="form-control" value="@ViewData["TenantId"]" />

                                <div class="form-group">
                                    <label asp-for="Messages"></label>
                                    <textarea asp-for="Messages" type="text" class="form-control" rows="3" placeholder="Enter your message"></textarea>
                                    <span asp-validation-for="Messages" class="text-danger"></span>
                                </div>

                                <input type="submit" id="SendMessage" class="btn btn-block bg-success" value="Send">
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</main>

<div>
    @Html.ActionLink("Back to Home", "Index")
</div>